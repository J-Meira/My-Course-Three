import { Typography } from '@mui/material';

import { PageTitle } from '../../Components';

export const CheckoutPage = () => {
  return (
    <>
      <PageTitle title='Checkout' />
      <Typography variant='body1'>Sorry! You need to be logged!</Typography>
    </>
  );
};
